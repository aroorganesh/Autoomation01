/*
Independent of your implementation, your script will get access to two objects:
 The context object that holds information about the current run and the commands object that has commands/shortcuts to navigate in the page.
*/
module.exports = async function(context, commands) {
  context.myId = 15;
}
